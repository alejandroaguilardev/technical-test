
## Configuration

ejecutar
```composer install```

Crear la base de datos ```gestlife.sql``` para la prueba 5 unicamente se esta usando el driver de mysql

Copiar el ```env.template  a un archivo llamado ```.env```, y reemplazar los valores por los correspondientes para su entorno de desarrollo.

## Nota 

Se ordenaron las tareas cada tarea tiene un answer.md de igual manera una respuesta en index.html